export const environment = {
  production: false,
  apiUrl: 'http://backend:8081/api', // Use backend external IP and port
  tokenName: 'authToken',
  origin: 'http://172.166.92.213' // Frontend external IP (if needed)
};
